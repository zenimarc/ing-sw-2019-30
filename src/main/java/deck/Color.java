package deck;

/**
 * Color is an enumeration used to distinguish rooms in the board and the color of the bullets
 */
public enum Color {
    RED,
    YELLOW,
    BLUE,
    GREEN,
    PURPLE,
    WHITE
}